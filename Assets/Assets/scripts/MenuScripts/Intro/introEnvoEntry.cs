/*using UnityEngine;
using System.Collections;

public class introEnvoEntry : MonoBehaviour {

	// Use this for initialization
	public float envoEntryDelay;
	void Start () {
		gameObject.SetActive (false);
	}
	
	// Update is called once per frame
	void Update () {
		envoEntryDelay += Time.deltaTime; 
		if(envoEntryDelay >= 5.30f)
			gameObject.SetActive (true);
	}
}
*/